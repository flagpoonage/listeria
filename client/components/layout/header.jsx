require('./style/header.pcss');

import React from 'react'; // eslint-disable-line no-unused-vars

const Header = () => (
  <header className="header">
    <div className="title">
      {'Listeria'}
    </div>
  </header>
);

export { Header };
