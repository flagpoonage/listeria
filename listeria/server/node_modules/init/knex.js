'use strict';

module.exports = require('knex')({
  client: 'pg',
  connection: {
    host     : '127.0.0.1',
    user     : 'vaas_admin',
    password : '123456',
    database : 'vaas_development'
  },
  pool: {
    min: 0,
    max: 7
  }
});
